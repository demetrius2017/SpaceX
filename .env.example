# Copy this file to .env and fill in values.
# Secrets must NOT be committed. The real .env is ignored by .gitignore.

# --- OpenAI ---
OPENAI_API_KEY=
OPENAI_PROJECT=
OPENAI_BASE_URL=

# --- Telegram Bot ---
TELEGRAM_BOT_TOKEN=
# Optional: a default chat id for manual sends
TELEGRAM_CHAT_ID=
# Enable Telegram Mini App button (1/0)
TELEGRAM_USE_WEBAPP=1

# --- Server / Public URL ---
# Public HTTPS base URL pointing to monitor_output/ (used by Telegram Mini App button)
PUBLIC_URL=
HOST=0.0.0.0
PORT=8000

# --- Database ---
# If using Postgres (e.g., on Heroku), set DATABASE_URL like:
# postgres://user:pass@host:5432/dbname
DATABASE_URL=

# --- Monitor config ---
# Enforce GPT-only mode (exit on failure)
GPT_STRICT=1
# Decay days override (optional)
DECAY_DAYS=7
